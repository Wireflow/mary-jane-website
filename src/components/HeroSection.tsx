import React from "react";
import Section from "./ui/section";

type Props = {};

const HeroSection = (props: Props) => {
  return <Section className="" backgroundImage="/img-hero.png"></Section>;
};

export default HeroSection;
